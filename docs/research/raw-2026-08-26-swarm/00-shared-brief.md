# Context (shared)
You are one agent in a research swarm improving Tofy, a ~460K-parameter Rust/candle JEPA-style recurrent world model for interactive ARC-AGI-3 games (64x64 grids, 16 colors, ~4k atomic actions incl. coordinate actions), trained on a single A40. Constraint: at play time the agent is fully self-contained (no LLM harness, no external code execution); deliberation through its own learned model IS allowed (LeCun-style latent planning is the north star).

Current state (v5 recipe, run "foundation-v2", in progress): lessons deleted in favor of one smoothly-annealed mixed stream; primary loss = per-pixel 16-way cross-entropy on the DECODED PREDICTED latent with changed-pixel upweighting (1-p)/p + copy-gate head + 1% unimix; Epps-Pulley anti-collapse regularizer budget-capped at 30% of prediction gradient with separate clip groups; same-state branch groups (up to 10 actions/state) with displacement push/pull + action-from-displacement losses; board size/placement randomization (7..32 on 64x64), color permutation + D4 augmentation with action conjugation, per-episode operator resampling; patch-4 latent grid, FiLM action injection each recurrent step; WSD schedule + EMA eval weights; Muon for matrices (degenerate ones on Adam); automated run gates. Live telemetry: at step 9216/24576 changed-pixel exact accuracy = 51.4% held out (v4 peaked at 16.6% and collapsed to 3.4%); action-sensitivity shuffled ratio 0.81 and improving; foreground reconstruction plateaued ~0.67 (decoder ceiling was ~0.60 in v4 — possibly still a binding ceiling). Planning: not yet implemented; docs/adr/0004-latent-planning-contract.md specs Phase A (rewire proven P1 "planning by falsification" controller onto latent imagination), Phase B (proposal head, goal-conditioned value, EIG ensemble, Gumbel sequential-halving), Phase C (in-episode adaptation).

Read for grounding (do not rehash them back at me):
- docs/adr/0003-world-core-v5-foundation-v2.md and docs/adr/0004-latent-planning-contract.md
- docs/research/2026-08-24-foundation-improvements.md and raw reports under docs/research/raw-2026-08-24/ and docs/research/raw-2026-08-25-planning/
- formal/TofyFormal/*.lean (10 machine-checked theorem groups: non-identifiability w/o branch coverage, copy-attractor threshold, CE-bounds-exact-match, EP marginal blindness, displacement separation, reactive-policy impossibility, ordinal sufficiency for greedy, symmetrization, mode-vs-mean Huber decode, locality)
- src/ for the actual implementation.

# Rules
- Inspect only this repo plus web sources. DO NOT edit, create, or delete any file in the repo; do not run cargo/training; do not ask questions; do not call subagents; do not create a plan file. Return your final report directly to stdout as Markdown.
- The bar is NOVELTY BEYOND THE CURRENT FRONTIER: the prior research waves already swept 2025-26 literature. Do not re-recommend what is already in the ADRs/reports (branch groups, pixel CE, EP budgeting, WSD/EMA, Gumbel search, PLDM/V-JEPA-2-AC-style MPC, ensemble-disagreement EIG, prequential TTA — all known). Cite literature only to establish where the frontier IS, then go past it.
- Every proposal must include: (1) precise mechanism (what tensor/loss/algorithm changes), (2) why it should work — an argument from theory, ideally reducible to a provable finite-math statement (Lean-formalizable) or a measurable invariant, (3) cost estimate on A40/candle at ~0.5-2M params, (4) a cheap falsification experiment (<=2k optimizer steps or offline on existing checkpoints), (5) expected effect size on changed-pixel exact accuracy and/or live ARC-AGI-3 score, (6) failure modes and how to detect them early.
- Rank proposals by expected-value-per-GPU-hour. Mark each as EXPLOIT (improves current run's lineage) or EXPLORE (new capability). Be blunt about which of the current v5 design choices you would bet are the NEXT binding constraint.
- Evidence discipline: cite file paths + line numbers for repo claims, URLs/arXiv IDs for literature claims, and label speculation as speculation.
