# Cohort-preserving known-control replay — preregistration

September 9, 2026 IST. Register before any replay training or replay checkpoint output. This is one empirical selection screen, not a proof of architecture quality. Source implementation may be prepared while the preceding frozen seen diagnostic is finishing; no training until its valid result is reviewed.

## Claim, comparator and premise

Does reordering the same 73,600 `(input,target,loop depth)` examples improve final four-loop policy and successor accuracy on the fixed known-control development panel, at the same initialization, 1,150 optimizer updates and numerical recipe? C5 at71ff87ad completed those updates but factual policy was15/64 (constant18/64) and all256 factual successors were goal templates. The next frozen72seen panel atae4597b6 probes seen-input behavior. Branch: if that valid panel fails either competence gate, run this optimization-order screen; if both pass, reconsider the intervention before spending compute. Hash/profile/population failure calls for repair, never a model decision.

Baseline is the completed C5 known-seed0 final checkpoint `e379802fa8e75327061f3dcc5ae95697f83e2d2d137a69ef68daf3f902040d41` and its64-query final-frozen output. It need not be retrained. Same local RTX5060 Laptop, driver, source-independent numerical learner, physical33/tail31/accumulation2/effective64. Wall-clock scheduling differs; do not make timing claims or ignore hardware changes.

Finite premise: the old effective64 batch comprises four query gradients repeated16 times. Under deterministic independent examples, its exact-real preclip mean equals the four-query mean. This is not a bitwise F32 or Adam trajectory equivalence. Conditional iid covariance Sigma/n does not establish actual trajectory variance. Current and proposed source schedule are exhaustively enumerable without training; no theorem guarantees a lower loss or better policy.

## Single intervention and invariants

At zero-based sample id, let u=id//64,j=id%64,d=u%3,n=(1536 if d==0 else1532),t=(64*(u//3)+j)%n. Select query episode `12*(t//4)+4*d+t%4`, with mapping0 and unchanged seed9173 XOR TRAIN_TAG. This enumerates the sorted original-depth cohort cyclically. Every full batch has64 distinct queries; each of4600 queries appears in16 different updates, at exactly its original training depth. Preserve the entire input-target-depth multiset, per-depth class totals and global1/2/4 loop schedule. Only grouping/repetition spacing changes, jointly; their separate causes remain unidentified.

Retain 992,393 parameters, hidden128/4heads/2 shared blocks/max8 loops; original initializer SHA256 `4cd502f7a76fe3dd9729f6693e9d707c670972f7ebe58ad1cbdde37ea2bb2802`; fresh seed0. Same losses (policy1,reward0.1,value0.1,balanced categorical dynamics0.5), AdamW lr0.0003/wd0.01/clip1, F32, physical33+31, effective64, 1,150 updates and73,600 samples. No resume, altered objective, extra training data, checkpoint choice, LLM or ARC access. Reference8queries/128forwards and its labels stay identical; final reference alone cannot promote anything.

CLI flag --known-replay is confined to known-mapping fresh Coverage/CoverageAudit with seed0, data9173, effective64, loops4 and no checkpoint/seen flag. Allow only updates1150 or3: three updates are explicitly implementation smoke. Source must label actual order and avoid claiming legacy stream order parity. The full CPU audit must match all original query/input/target/label identities, multiplicities and depths; the actual training stream must match that audit byte for byte.

## Frozen evaluation and statistics

Reuse C5 development panel:64 unique queries, seed20260912, tag0x4b4e4f574e, mapping0, distance1, ordinary factual plus cleared support, four loops, batch1. It is already observed and helped select this intervention; label the result selection-only, requiring fresh held-out confirmation and multiple seeds before method promotion. It is not public ARC data. The query multiset is unchanged so audit must still find zero overlap. Baseline and treatment must consume identical raw input/target identities, with per-frame oracle reconstruction and exact raw readout integrity.

Primary policy: factual accuracy/CE, best constant baseline, and paired replay-minus-C5 accuracy. Primary successor: factual full-frame exact overall and separately blocked/nonterminal/terminal; copy/template exact baselines and fraction predicted as the goal template. Also retain changed/unchanged/vacated/destination, reward precision/recall/Brier threshold0.5, valueMSE versus constant1 baseline0, and cleared counterparts as independent diagnostics. Cleared known-control behavior has no25% information ceiling. Do not conflate accurate pixels or sensitivity with correct state transitions.

Use10,000 paired whole-query PCG64 resamples seed1912,95% linear percentile intervals; all four actions and both conditions stay clustered. Recompute best constant in each draw. Track zero-denominator draws and return undefined intervals if any occur. No metric/checkpoint/seed subset selection after observation. Degenerate empirical intervals are not population proofs.

Candidate continuation rule (not promotion): factual policy>=90%, lower95%CI of policy advantage over resampled best constant>25pp, lower95%CI of paired accuracy gain over C5>10pp, and successor full exact>=75% overall and every nonempty outcome stratum. All conjunctive. Cleared/reward/value/reference cannot rescue a failed primary gate. One seed cannot establish robustness. A pass selects fresh-panel/multiple-seed confirmation; a failure rejects this ordering recipe for scaling and requires a separately registered representation or objective diagnosis. No unregistered dependent training.

## Checkpoints, runtime and profiling

Train exactly1,150 updates, save every25 and final; only final1150 is evidential. Intermediate metrics/checkpoints are operational diagnostics, never quality stopping/selection. Budget<=2100s model runtime (C5 measured1866s including supervisor overhead), plus<=600s external profile finalization. Three-update smoke and each frozen evaluation<=300s plus external finalization. Reserved time for integrity, analysis and artifact sealing. Stop immediately for nonfinite/integrity/profile mismatch or sampled wholeGPU reserve<512MiB. Do not silently shorten/extend the schedule on a slow run; preserve failed provenance and decide separately.

Launch only a clean reviewed pushed commit with dependency1ea5cc5c5998a1c2bd388eb158653dd0d1b7452a, separately hashed binary built `cargo build --release --locked --offline --features cudnn,profiling,serde_json/float_roundtrip --example looped_agent_probe`. Verify exact-binary CPU audit and3-update Coverage smoke at33+31 through loops1/2/4. Batch33 is the largest prior stable physical batch (35OOM,34 failed reserve); unchanged shapes preserve that candidate, smoke checks the new binary/order. Do not retry34 and knowingly violate the existing limit.

All supported profilers remain on: unique host trace,NVTX,Nsight CUDA/cuDNN/cuBLAS/OSruntime plus CPU sampling; Candle Graph first evaluation and training updates2,100,1000; smoke captures first evaluation andupdate2 while its third update exercises maximum training depth4. Frozen evaluators capture their first forward. Retain original capture plus separately published Nsight-bound bundles, officialCSV reports and capture manifests. Before full training, verify actual smoke and initial legacy frozen outputs; baseline initial raw panel must match C5 byte for byte. Check all numerical helper/model/task hashes and exact initial weights. No partial coverage described as full: operation/activation linkage, allocator lifetimes/physical memory checkpoints, device-event intervals and complete automatic NVTX correlation remain unavailable. Timing is profiled work, not production performance.

Each invocation gets a never-reused root and supervised process IDs/telemetry. Before the final frozen handoff, validate all1150 updates, exact source/checkpoint/stream/manifest, parent shutdown and fetch authentication/exact-commit availability. A driver may automatically run only the preregistered final frozen evaluation, binding and analysis after those machine-checkable prerequisites; it must stop on a failed prerequisite and cannot select another training intervention. Do not replace a binary still in use. Stop children/telemetry, hash and reverify the finalized campaign, retain digest outside root, publish results/insight with source and negative outcomes. Completing this screen does not mean the user's broad model objective has been achieved.
