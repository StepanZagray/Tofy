# C16 bounded learned action-binding component

September 9, 2026, 19:21 IST. C15 verified exact entity locations and demonstrated displacements for both frozen pairs on1024reusedrows, without any policy gain. This new route explicitly separates location extraction from learning which action has the desired effect. It retains the user's shared-depth reasoning/no-LLM constraints.

Implementation hypothesis: a small from-scratch transformer can consume three public observed action-effect records and one desired-effect query, then learn four-action choice under held-out bijections. Each record has7features: dx,dy,one-hot action0..3,queryflag. Query has zero action-ID features and queryflag1. The intended future visual adapter computes coordinate expectations from learned role attention and before/after differences; it does not use simulator controls or hard argmax. Initial test inputs may be exact abstract vectors, but those outcomes must be labeled abstract binding, not image-based policy performance.

Implement only a ControlBinder model and meaningful unit tests now. Width64, two shared TransformerBlocks, four attention heads, max8loops, initial zero state, recalled input injection once per loop, existing RMS/SiLU residual block math, learned CLS token, final RMS and learned four-action head. No positional encoding: action-effect records are a set, and support permutation invariance is desired. Input projection maps7features to64channels. Default experiment budget/depth will be separately registered. Expose existing TransformerBlock/rms_norm within the module tree rather than duplicate or change the vision core's math. No solver, control table, simulator, delta-target labels or fitting routine belongs in the learned model forward.

Tests must establish shape/bounds, recurrent parameter sharing, nonzero finite gradients through input and shared blocks, distinct-loop computation, and support-record permutation equivariance/invariance within numerical tolerance, while all old model tests keep passing. This is a component implementation contract only. Training population, optimizer, seeds, checkpoints, baseline controls, budgets, promotion/stop gates, source/binary and profilers require full registration and review before any numerical campaign.

Mathematical premise to verify independently: for four cardinal unit effects that form a bijection, three observed effects determine the missing fourth. A finite polynomial rule exists as an analytic positive control, but will not be installed as the learned policy. Existence of such a rule is not a convergence theorem for this transformer/optimizer. For normalized spatial probabilities on an8x8grid, expectation error per coordinate is at most7(1-p_true); a before/after difference error is bounded by the sum of endpoint errors. Floating normalization and unknown tasks require explicit scope qualifications before using that bound.

## Superseding user direction before model execution

The initial width64 component above was implementation-only. The user subsequently requested at least1million parameters and effectivebatch512. Current registration now specifies width256/fourheads/two sharedblocks,1,580,804parameters, and effective512. The learned-input seam and existing block arithmetic remain the same. No100k model campaign or CUDA qualification was run, and no small-versus-large empirical claim can be made.
