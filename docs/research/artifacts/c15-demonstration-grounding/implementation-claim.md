# C15 implementation claim and next empirical prerequisite

September9,2026,18:43 IST. Parent C14 completed a valid negative fixedridge screen; its regularizer does not exhaust the affine function class. The selected new route investigates a different representation seam, before/after demonstration frames, rather than claiming upstream information absence or selecting another output-only fit.

Implementation claim: expose exact post-final-RMS features for each of the seven existing observed frames, with six alternating support before/after frames followed by the query. A small frame(index) interface must preserve original CLS/current features and all legacy numerical/gradient behavior, return contiguous[B,64,hidden] tensors, reject invalid indices, and retain autograd connectivity for future training. No learned parameters, attention mask, normalization, recurrent depth, data/preprocessing or core tensor computation changes. Retaining the existing final sequence handle may extend its lifetime; document that memory effect. Test the implementation before any numerical experiment.

Next empirical claim, requiring a separate full registration before launch: current learned role-query grounding may transfer to each support before/after frame on the existing seen factual C12 population. Frozen initial and C12-final checkpoints plus their own heads are the comparator set. Apply the same head separately to each exact frame tensor; public pixels produce role/movement audit fields for scoring only. The current frame must reproduce original C12 outputs on identical rows/batches. An analytic uniform-attention negative control is zero-accuracy by construction because cell0 is a wall; it is not an empirical policy. No fitting, hidden map inputs or controller action claim.

Why it matters: a later learned transition-binding pathway needs grounded before/after entities. Correct current-frame role selection alone never established that prerequisite. If support grounding is poor, improve or supervise shared perception before introducing learned action-effect tokens. If it succeeds, separately register a differentiable temporal representation and recurrent control-binding test. Existing contextual features have an architectural path to every frame, not a guarantee of correct semantic information. A zero-forward mathematical guarantee of role transfer is unavailable; a per-frame selector can fail under frame-role/time context shifts even when current-role margins are large.

Budget and scope: only bounded CPU implementation checks are presently authorized by this implementation record. Numerical population, metrics, seeds, controls, promotion/stop gates, exact source/binary and profiler details will be frozen before a CUDA diagnostic. No long training, public ARC evaluation, architecture optimality, novel LLM/controller, or use of hidden simulator roles at inference is implied.
