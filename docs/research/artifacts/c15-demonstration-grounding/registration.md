# C15 frozen demonstration grounding registration

Registered September 9, 2026, 18:55 IST, before any C15 numerical model invocation or support-frame outcome. This updates the implementation-only claim with the narrow operational decision below. Prelaunch review corrected the support topology described below; metrics and thresholds did not change.

## Claim, scope and derivation

Test whether each existing checkpoint's own learned spatial role queries can be reused on the six demonstration frames. Correct current-frame selection does not entail correct support-frame selection: time/frame metadata and contextual attention may change feature coordinates. There is no applicable guarantee. This is an empirical prerequisite for this particular selector-based representation, not a prerequisite for every possible transition representation.

Two frozen arms: C12 initial core SHA256 4cd502f7a76fe3dd9729f6693e9d707c670972f7ebe58ad1cbdde37ea2bb2802 with its privileged C10 canonical head a43ad78fe7a6b1594d23c205fb191c8ac81610deaaff25198cb421d423edc678; C12 final core dc732f17a38f7bc6510dc6480a61dc4862fcb7c89ae1560f3a06515b8785d93f with own head 37675528ef00055f16a17e826648078ff042492787087f3604d58cf0efc60fdf. Compare initial and final as complete core/head pairs; no body-only causal claim.

Population is exactly the sealed C12 seen factual panel: 64 query groups, each with 16 specified fit bijections, 1024 rows, seed 20260920, original training-group index floor(i*4599/63), i=0..63. Map order [0,2,4,5,7,8,9,10,13,14,15,16,18,19,21,23]. Existing C12 audit identity must match exactly. These are reused training queries, not held-out confirmation. No ARC data, generator search, new seed selection, or new fits.

## Intervention and invariants

Expose exact existing post-final-RMS frame slices in input order: before0, after0, before1, after1, before2, after2, current. No learned tensor operation, weight, normalization, preprocessing, attention mask or shared-depth computation changes. Four loops of the same two-block, width128 core. Apply each arm's unchanged SpatialPolicy to each slice, reuse ordinary current output at frame6. Only public pixels and metadata enter the model; role locations, movements, controls and labels enter scoring only. Zero optimizer updates, no losses/gradients in the frozen forward. Verify all parameter tensors remain bit-identical. Retaining the final full-state handle can extend allocation lifetime; support slices are materialized only on demand.

Each output row retains all C12 audit fields plus checkpointstage, current logits/attention/pooled unchanged. Add frames[7], each with public cells[64], attention[2][64] and pooled[256], plus one row-level public_metadata[4480] array. Reconstruct U32 patches by repeating each cell64 times, concatenate F32 metadata, and verify the exact sealed input hash and separate query/metadata hashes. Validate unique color2 agent and color3 goal. Query/current frames have boundary walls; calibration support frames have no walls and goal63. Validate actual support topology, sequential nonzero cardinal displacements, unchanged goal and all other cells. Public action IDs and audited controls may cross-check outcomes in the scorer, never enter a new inference path.

## Metrics, controls and uncertainty

For both arms, all seven frames and both roles report top1 accuracy (first index breaks ties), true-cell attention mass mean/minimum, joint-role accuracy per frame, and all-six-support-frames-both-roles-correct fraction. For each of three support pairs, derive predicted agent displacement from the two learned attention argmax positions and compare with the public-pixel displacement. Report per-pair and pooled exact displacement accuracy, before/after both-agent-correct fraction, and all-three-displacements-correct fraction. Correct displacement can occur despite wrong absolute positions; keep those separate. This deterministic readout is a diagnostic of selected locations, not a learned action policy.

Analytic positive scorer control: one-hot attention on public role locations must yield every role and displacement correct and mass1. Analytic negative: uniform64 attention yields first cell0, which is a query boundary wall or non-role support floor, hence zero role accuracy and zero nonzero-displacement accuracy by construction, mass1/64. Verify absence of roles at cell0 for all rows before asserting the negative control. Neither control is a controller result. Include unchanged current action accuracy only as parity, not a new policy claim.

Uncertainty: 10,000 paired whole-query-group bootstrap draws with NumPy PCG64 seed1944, resample64 groups retaining all16 maps and7 frames. Percentile linear pointwise95% intervals on accuracy, joint correctness and mean mass, plus final-minus-initial paired differences. Fixed checkpoints/maps conditional estimand; no training-seed or map-population uncertainty. Do not bootstrap minima. No multiplicity-adjusted significance claim, seed subset, best checkpoint or best frame selection.

## Operational gate and next decision

For each arm, selector reuse is supported on this panel only if every support frame has >=99% accuracy for both roles, >=98% joint-role accuracy, and each of three support displacements has >=98% accuracy. Report every component; these engineering thresholds do not promote a policy. A miss rejects direct reuse of that arm's selector for this proposed pathway, not information presence in the trunk. A pass permits designing a separately registered learned before/after binding experiment; it does not establish map understanding or downstream success. Attention mass is diagnostic and not an additional pass gate. No automatic training follows either outcome. Initial/final comparison is exploratory and has no promotion gate.

## Parity and fail-closed integrity

All existing row fields and order must match corresponding C12 seen output identities. Current logits, attention and pooled arrays must satisfy abs(new-old)<=1e-5+1e-5*abs(old), with exact action and role argmax agreement. New frames[6] attention/pooled must be bit-identical to top-level current arrays. Reject nonfinite arrays, invalid probabilities or sums outside1e-5, malformed public inputs, hash/source/checkpoint drift, extra/missing rows, changed parameters, failed qualification or incomplete wired profiler evidence. Integrity failure cannot decide the scientific gate.

Before the two numerical invocations, run the existing bounded qualification at physical batch4 on the exact new CUDA binary and compare all four output rows (CLS/current/head tensors) with the sealed C12 batch4 qualification at the same tolerance, exact winners. CPU model tests cover exact legacy forward/gradient parity and all-frame indexing/differentiability. Independent scorer review must precede outcome analysis.

## Compute, provenance and lifecycle

Each frame arm uses physical batch34 plus tail4, matching C12 evaluators; accumulation is inapplicable with zero updates. No new training capacity sweep is useful for this matched frozen diagnostic. Maximum model runtime120seconds per qualification or frame arm; supervised invocation deadline600seconds for profiler finalization, total campaign budget20minutes after build. Expected frozen model phase roughly tens of seconds from C12/C13, not hours. Build with --release --locked --offline --features cudnn,profiling,serde_json/float_roundtrip. Source must be reviewed, committed, pushed, clean and embedded in the exact separately hashed binary before launch. Bind parent C12 outer seal, audit, checkpoint/head import, reference streams, operator/scorer code, Python/NumPy/BLAS runtime, sibling candle_graph revision, configuration and registration in the launch specification.

Use all wired profilers on first evaluation batch: candle_graph semantic spans and labelled frame/seam statistics, host scalars, TOFY_PERF_TRACE, matching NVTX, Nsight CUDA/NVTX/osrt/cuDNN/cuBLAS and CPU sampling. Evaluation has no gradient work. Bind official Nsight reports to new finalized bundles. Known gaps remain operation/activation linkage, allocation lifetimes, instrumented physical-memory checkpoints and device-event intervals; host duration is not kernel duration, sampled telemetry is not allocator peak. Retain profiled_work classification, no production timing claim. Qualify actual capture completeness/capabilities before outcome runs.

Record exact PID/start identity for every subprocess. AC power required, GPU under85C, at least512MiB free VRAM, no competing training. Stop and reap child/telemetry processes before sealing. New never-reused dotless roots, failed roots preserved, source/hash checks before and after. Only after frozen scorer and independent review agree, seal final artifact tree, retain external manifest digest, publish all positive and negative results with exact revisions, and clean up all task processes. No paper-faithful claim; this is a project diagnostic.
