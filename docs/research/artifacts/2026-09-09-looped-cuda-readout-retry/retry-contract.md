# C11 infrastructure retry

The original C11 source a2f1125d15bb5fff2f86f8a47887aeb7e575973d passed all eleven CUDA qualification invocations. CPU panel0 audit generated256 rows, zero collisions, then failed the reused GPU-oriented verifier because its valid, hash-bound host trace was the empty JSON array. Failed campaign manifest: a534bd999136be79e7a8cb962bb89a9371eceecc7bf60c44bf152388546ba508. All211 tracked process identities were absent when sealed. No fresh model inference or new-panel metrics were accessed.

This is an infrastructure retry in a never-used root. Keep the original registration byte-identical: no fitting, no model/generator/head changes, no seed replacement, no changed thresholds/tolerances. Two infrastructure corrections are required: verify a zero-forward CPU audit through its exact artifact/trace/provenance contract, and preserve each root manifest hash in the analyzer report. The original analyzer validated the correct root hash, then reused the variable name while checking input hashes and returned the final input hash as manifest_sha256. Raw numerical/source checks remain intact, but the original report attribution is wrong and must not be silently repaired. GPU captures still require populated evidence; numerical methods and decision gates are unchanged. Re-run device qualification from reviewed pushed source. The same fixed panels will be regenerated and must match the retained first audit bytes; panel0 has been accessed for data validation only. The other two panels remain unaccessed. Qualification/export/source seals and the finite campaign budgets are recorded anew, while original costs/failure remain separately disclosed.
