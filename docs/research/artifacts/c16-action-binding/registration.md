# C16 learned action binding registration

Registered September9,2026 in machine-local IST, before any C16 learned-model campaign or numerical outcome. Implementation-only unit tests and finite analytic/data checks may precede this record. No C16 learned checkpoint has been evaluated or selected.

## User-directed prelaunch revision

Recorded at20:10IST on September9,2026 after the user requested at least1million parameters and effectivebatch512. This replaces the100,292parameter/batch64 implementation-only proposal before any C16CUDA/model campaign. The revised model has1,580,804parameters at width256; effectivebatch512 and1150updates consume588,800presentations. No comparison against a trained smaller model is claimed. Both capacity and batch changed by user instruction; optimizer,task,split,seed,depth,gates and terminal-checkpoint policy stay fixed. The previous registration and preparation datasets remain retained as superseded prelaunch artifacts.

## Claim and mathematical boundary

Can the fixed1,580,804parameter ControlBinder learn content-based action retrieval and set complement for four cardinal action-effect bijections, including the eight held-out mappings? This is a controlled shared-depth reasoning prerequisite, not a claim of optimal architecture, general rule induction, superior recurrence, visual learning or ARC performance. C15 established exact frozen selector locations/movements on its reused panel while native policy remained25%; this motivates making observed effects explicit.

Analytic existence: with three distinct observed action indicators A[3,4], cardinal vector effects E[3,2], and desired cardinal vector q[2], O=A^T E, m=1-A^T1, F=O-m(sum_rows E)^T reconstruct every effect because the four cardinal vectors sum to zero. Fq has one1, two0s and one-1, hence correct-action margin1. This analytic solver is only a scorer positive control; it is absent from the learned model. The construction fails outside its cardinal-bijection/three-distinct-evidence assumptions. Existence is not an AdamW convergence theorem. The omitted-subset shortcut alone predicts the absent action correctly100% there and0% on demonstrated cases,25%overall; both subsets must pass the learned-model gate.

For normalized role attention on grid coordinates0..7, coordinate-expectation error in each axis is <=7(1-p_true). Before/after and goal/agent difference errors add. The fixed cached visual panel uses INITIAL C15 attention only, normalizes its64weights inF64, computes expectations/differences then rounds features once toF32. Its nearly-one-hot privileged initialization makes this bound informative; diffuse C12-final mass does not. The cached diagnostic has no fresh abstract control cases and cannot prove learned-binder robustness or native end-to-end inference.

## Data, effective population and access

Enumerate all24lexicographic control permutations. Fit IDs are exactly[0,2,4,5,7,8,9,10,13,14,15,16,18,19,21,23]; held-out IDs are[1,3,6,11,12,17,20,22]. For every map enumerate24ordered triples of distinct observed action IDs and four desired directions in order up,down,left,right. Row order is observed-triple-major, desired-direction-major, map-slot-last. There are1536ordered fit rows and768ordered held-out rows, but the model is a set function of the three support records: six orderings share one semantic input. Report256fit and128held-out semantic orbits explicitly. Canonical representatives sort the three action IDs ascending; gate scores use these256/128representatives. Require exact action-winner agreement across all six orderings in every orbit; report maximum logit deviation descriptively. Never treat ordered duplicates as independent samples.

Three support records are[dx,dy,onehot_action0..3,queryflag0]; query is[desired_dx,desired_dy,0,0,0,0,1]. Only the resulting F32[4,7]features enter ControlBinder. Hidden map IDs, labels, grouping IDs and source audit fields enter generation, CE or scoring only. Rust and independent Python checks bind feature and label hashes, row ordering, cardinalities and complete orbit membership. The finite synthetic pretraining distribution contains no ARC game data.

Fixed schedule: NumPy PCG64 seed20260922 shuffles all1536fit rows independently each epoch, batches512, consumes1150updates=588,800presentations:383complete epochs and512rows of epoch384.512ordered rows appear384times and1024appear383times. This averages2300presentations per semantic orbit; repeated presentations do not expand the finite semantic population. Dataset/schedule and software hashes freeze before model work. No fitted preprocessing, augmentation selection, new map split or threshold chosen after results.

The optional-by-name but fixed-in-this-registration cached evaluation is all1024C15initial rows, whose source61e1335670239627772088a2fb6c81c2df499ed2 and completed outer6007293c5f51d263b0e3f40dcd819124efa8ffdaf2809202eda0c60142ffd40d are bound. Features derive from learned attention and public action IDs; scoring may inspect original public cells/roles and labels. No true locations, map IDs or desired direction labels enter these features. This is a reused cached visual-seam diagnostic, with role-supervised initial queries; it is not held-out vision/control evidence and has no promotion gate.

## Model, initialization and optimization

ControlBinder has7→256projection, learned CLS, two existing shared width256 RMS/attention/SiLU residual blocks, four attention heads of64channels, no positional encoding, and four-action linear readout after final RMS. State starts at zero and the recalled projected inputs are reinjected once per loop. Train with4loops, allow1/2/4/8 for frozen depth diagnostics. Existing vision-model block operations are unchanged; the visual core is not executed in this campaign. No pretrained LLM, external solver, memory, planner, reward, value or world-model objective is present.

One preregistered seed0 screen. Initialize every named parameter deterministically using the existing looped_agent_probe initialize algorithm: SHA256(seed-u64-LE || parameter-name), first8digest bytes as little-endian ChaCha8seed; biases0, rank1weight1, other weights uniform with bound sqrt(6/(first_dim+last_dim)). Sort parameter names; record canonical names/shapes/F32-bit digest and exact initial checkpoint. Constructor randomness is fully overwritten. Exact initial parameters must match qualification, disposable batch tests, frozen initial evaluations and real training.

Train1150updates, effective512. AdamW lr0.0003, betas0.9/0.999, epsilon1e-8, weight_decay0.01, global L2clip1, no LR schedule, CE only. Mean CE over512 examples; correctly scale microbatches before accumulating only parameter gradients. Sort optimizer variables and clip reduction, use existing validated helpers. Largest permitted physical batch is512 because effective batch is512. Qualify512 with2disposable updates, then5update confirmation and exact restore; if it fails only for diagnosed OOM/reserve, try256then128then64then32then16then8then4then2then1, each with the same restored initialization and effective512. Every candidate divides512 exactly. Stop on other failures. Select the largest passing physical batch, minimum accumulation, and use it unchanged for training. Record both selected values. CPU/unit checks do not count as accelerator qualification.

## Qualification, captures and budget

Launch only from reviewed, pushed, clean source with sibling candle_graph1ea5cc5c5998a1c2bd388eb158653dd0d1b7452a. Build exact separately hashed binary using --release --locked --offline --features cudnn,profiling,serde_json/float_roundtrip and a unique recorded TOFY_BUILD_COMMAND. Bind source, build logs, feature flags, dataset/schedule, scorer/controls, Python/NumPy/BLAS, config and registration hashes. CUDA F32, TF32 override0, AC power, temperature<85C and >=512MiB VRAM reserve. Run the exact binary's four-row frozen qualification first; finite outputs/CE, correct source/device and complete supported capture are required. It is implementation smoke, not learning evidence.

All supported profilers remain enabled. Capture the first selected qualification/evaluation batch, batch-smoke update1, and real training updates2,100,1150. Use candle_graph semantic spans, input/logit/loss tensor stats, host scalars and all parameter gradients with declared active input/shared_core/policy families before clipping; matching host trace/NVTX and Nsight CUDA,NVTX,osrt,cuDNN,cuBLAS and CPU sampling. Bind new finalized Nsight bundles with available/bound GPU provenance and explicit raw application-label validation. Existing automatic-correlation, operation/activation, allocation-lifetime, instrumented memory and device-event gaps remain disclosed. No visual model ran here; cached features retain separate C15 producer provenance. Profiled_work and host span times do not establish production throughput or kernel cost.

Model limits: qualification/evaluation/disposable smoke120seconds each, real training600seconds. Five-update confirmation admits training only when1.15*(1150/5)*measured_update_seconds plus measured checkpoint overhead <=600seconds. After preparation/build, entire numerical campaign has a hard30minute wall budget with child-process timeouts clamped to remaining time. Reserve room for independent scoring/sealing; stop after the fixed terminal checkpoint, no best-checkpoint selection. Only initialized and terminal weights are model evidence. Errors/nonfinite/changed source/failed profiler/hash/cleanup are integrity failures, not negative science. No automatic new recipe or additional seed follows this screen.

## Frozen evaluations and comparators

Initial binder: fit and held-out factual at4loops. Terminal binder: fit and held-out factual at1,2,4,8loops; both support-effect-zero and query-effect-zero controls at4loops; cached C15initial visual features at4loops. Evaluate fixed streams in canonical source order; zero optimizer updates and unchanged parameter digest. Effect-zero changes only first three records'dx,dy; query-zero changes only final record's dx,dy; retain public action IDs/query flag. Never combine the two ablations. For each control, the relevant identical-input group has exactly balanced four labels, so overall25% follows by construction for any deterministic classifier; report subset behavior separately. Always-missing-ID and analytic exact solver are external scored controls, never selected policies.

For every stream report raw and canonical correct counts, CE, action histogram, per-map accuracy, demonstrated/omitted counts, complete six-order winner consistency, and margins. At4loops report paired initial/terminal and factual/ablation differences on identical semantic cases. Final depth diagnostics have no selection role and do not prove a benefit of shared depth: training depth stays4, no matched alternative architecture is trained. Exact finite-population counts have no sampling CI; uncertainty about training seeds, task distributions and unseen map classes remains unknown. No IID/binomial or duplicated-row bootstrap claims.

## Decision and stop rule

Feasibility supported only if terminal4loop canonical fit accuracy>=99%, canonical held-out overall>=90%, held-out demonstrated>=90%, held-out omitted>=90%, and every final factual4loop semantic orbit in fit and held-out has the same action winner under all6support orderings. Integer minima are254/256fit,116/128held,87/96held-demonstrated,29/32held-omitted. Initial and other-depth orbit discrepancies are reported descriptively and do not select or veto the4loop scientific gate;4loop ablation identical-input winner determinism remains a separate control integrity check. All analytic/data/initialization/gradient/source/device/profiler/checkpoint/cleanup gates must pass independently. A valid miss rejects this seed/budget/recipe screen, not the entire architecture class. A pass supports only the bounded learned retrieval/complement prerequisite; promotion requires new seeds and harder distributions, and native visual integration/fresh ARC evaluation remain separate.

Primary and independent scorers freeze before outcomes and must agree on counts, CE/margins, ablations, orbit consistency and all gates. Report all results, including failed depth/omitted/cached cases. Stop telemetry and descendants before externally sealing the finalized tree, retain manifest digest outside the run root, publish a concise ml/tofy insight and RESULTS_P2 update, and identify/clean exact task processes. This screen never authorizes a public score claim or a hardcoded action matcher in Tofy.
