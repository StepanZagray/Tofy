# C13 frozen query-sharpening preregistration

Registered September 9, 2026, 17:55 IST before inspecting sharpened attention or action outcomes. This is one exploratory sufficiency diagnostic on reused C12 seen examples, not promotion or fresh confirmation. The factor 16 was selected from the C12 endpoint observation and theoretical power identity, not a temperature sweep.

## Question and mathematical prerequisite

Does multiplying only C12 terminal policy queries by 16 restore accurate control predictions with its terminal core and affine decoder fixed? Baseline is the retained C12 final-seen-factual stream: 254/1024 correct, CE1.4059184908353308; all role winners correct, mean target attention masses .196754812176/.209896332599. No counterfactual action outcomes have been inspected.

For finite scores s_i and alpha>0, softmax(alpha*s)_i = p_i^alpha / sum_j p_j^alpha, p=softmax(s). Thus positive scaling preserves exact unique argmax and increases its mass. This algebra proves only the concentration mechanism, not correct actions; contextual features and the trained affine decoder may use distributed information or have adapted to diffuse pooling. Float32 implementation is checked numerically. Complete current-token features were not retained, so changed pooled/logit values require one actual frozen forward.

## Fixed intervention and invariants

One factor: queries *=16, exact power-of-two F32 scaling, no overflow. Read exact strict three-tensor terminal head; retain header/layout and output.weight/output.bias bytes unchanged. Core file byte-identical. No gradient, optimizer, recurrence-depth, support, seed, sample, metadata, label, batch or preprocessing changes. Reuse source 5b17246cef110a866bdb8c2d2b14919710256ff3 and binary SHA256 60c677a48cdf297692d5f08c978da6926c18aa7d1a27e632162ed6416079b257 from clean pushed Tofy-grounded-training checkout. CandleGraph revision1ea5cc5c5998a1c2bd388eb158653dd0d1b7452a. Terminal core SHA256 dc732f17a38f7bc6510dc6480a61dc4862fcb7c89ae1560f3a06515b8785d93f and original head37675528ef00055f16a17e826648078ff042492787087f3604d58cf0efc60fdf. Parent outer seal0a4ac9a8d3f46ebfca141360384a18761ea4c9fd175c43176788dd422ec7ab6f.

Exactly existing seen factual64 groups ×16 fit maps =1024 rows; same row order, audited cells, inputs, labels and metadata. Use existing C12 audit, physical batch34 and tail4 for eval, four loops, zero optimizer updates. No other cohort, clear condition, temperature, refit or checkpoint is eligible. Public ARC levels are excluded. C10 privileged role warm start remains a material limitation.

## Cheapest premise before GPU

Read only parent seen attention/audit identities, compute the fixed p^16 distribution in stable F64 log space. Require finite nonnegative normalized input probabilities, correct unique role winners on all rows, predicted minimum target mass>=.99 for both roles on every row. If unmet, stop this registered diagnostic as premise-not-met; do not tune alpha. Serialize and independently compare the scaled head tensor bytes before any GPU call. Verify parent seal files/bindings before execution.

## Evaluation and scoring controls

One frozen CUDA eval_final invocation with max_seconds120, physical34; reuse exact binary's C12-qualified CUDA backend and all wired profilers: one representative evaluation capture, CandleGraph semantic/tensor/host evidence, Chrome/NVTX and Nsight CUDA/cuDNN/cuBLAS/OS-runtime/CPU sampling. Bound and verify the new bundle; no gradients expected at zero updates. Operation/activation linkage, allocation lifetimes, instrumented physical-memory checkpoints and device-event intervals remain unwired. Raw application-label matching handles incomplete automatic correlation. Sampled VRAM is not allocator peak; profiled_work is not production timing.

Require same final core and unchanged output weight/bias bytes, new head independently parsed, zero updates, exact all audit fields and input sequence, finite outputs, all correct role winners preserved. Match actual attention to normalized parent p^16 with elementwise atol1e-4 and rtol1e-4; actual target mass minimum>=.99. Independently reproduce all action logits from retained pooled256 and unchanged affine decoder to atol2e-5+rtol2e-5. No reconstructed oracle inputs enter the model. Affine reconstruction only verifies execution, not support binding.

## Registered descriptive metrics and decision

Report action correct/1024, histogram, CE, correct-all16-maps group count/64, demonstrated/omitted subsets, role argmax and target mass min/mean, agreement controls, resource/provenance gaps. Score baseline and treatment against independently reconstructed labels from visible cells and audited bijective controls. Report accuracy and CE paired deltas, and accuracy versus exact25% constant. Bootstrap10,000 complete64-query resamples with PCG64 seed1940, shared indices across contrasts, linear pointwise95% quantiles. Intervals condition on the fixed16 fit maps, frozen checkpoint and selected seen-query population; they do not estimate uncertainty over unseen maps or training seeds. No multiplicity/promotion claim: one fixed exploratory contrast, secondary diagnostics descriptive, no selective subgroup promotion.

If all integrity/concentration checks pass and treatment accuracy>=.90 and paired lower95% accuracy delta>0, mark frozen_sharpening_recovers_registered_fit. If not90% but lower accuracy-delta bound>0, mark partial_accuracy_gain_only. Otherwise mark concentration_insufficient_for_accuracy_recovery. If integrity fails mark failed_integrity; if CPU concentration premise fails mark premise_not_met. Do not reinterpret failed controls as negative model evidence. Neither outcome identifies original cause or establishes absence of support information. No automatic training follows this parent; analyze before defining the next test.

## Budget, launch and stopping

One CPU premise (<=120s), one CUDA model phase<=120s plus finalization<=120s, binder<=120s, analysis<=120s; aggregate execution/analysis wall<=600s from premise start, excluding prior implementation/review. Register code and hashes before premise, review/push the operator snapshot in a separate diagnostics worktree while preserving the embedded numerical source checkout at5b17246c. Recheck AC, temperature<85C and at least512MiB sampled reserve. Track and clean every owned process; never reuse failed roots; stop on any integrity mismatch, resource violation or deadline. Seal completed artifacts and record the digest externally. Single seed, reused training-query subset and fixed privileged warm start are selection/exploratory evidence, not independent scientific promotion.
