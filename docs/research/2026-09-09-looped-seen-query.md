# Frozen seen-query prerequisite — preregistration

September9,2026 IST, before seen-panel model output. This is an evaluator-only diagnosis selected after the completed C5 negative result, not independent confirmation of an architecture and not a training intervention.

## Claim and premise

Does the final C5 known-control checkpoint solve a fixed temporally stratified subset of its training queries at the same four-loop inference setting used for the failed new-layout panel? The first8 reference queries have labels [64,0,48,16] over128 repeats, so their50% score equals a constant-action baseline. The whole-training fitting level and recency pattern are unknown. No mathematical argument fixes this checkpoint's scores; query selection is independent of model outputs.

Exact-real preclip duplicate gradients reduce the current effective64 batch to four distinct query gradients under deterministic independent computation. Exhaustive CPU enumeration establishes a cohort-preserving alternative with64 distinct queries/update and unchanged input-target-depth multiset. This does not prove a learning benefit. The frozen check precedes that possible ordering screen.

## Frozen design

- Baseline: C5 initial weights SHA2564cd502f7a76fe3dd9729f6693e9d707c670972f7ebe58ad1cbdde37ea2bb2802.
- Tested checkpoint: C5 final update1150 SHA256e379802fa8e75327061f3dcc5ae95697f83e2d2d137a69ef68daf3f902040d41, training source71ff87ad76400e977e8e3031afa437727af73ed5.
- Source campaign /home/stepan/Projects/code/.tofy-runs/looped-known-mapping-20260909T095023-IST. Accept only its sealed complete manifest and identical audit/training streams.
- Fixed72 query IDs: early0..23, middle2280..2303, late4576..4599. Each temporal block has24 queries,8 each trained at depths1,2,4. No label-based balancing, omissions or substitutions.
- Exact training generator seed9173 XOR0x545241494e, permutation0, min/max distance1, normal factual calibration. Assert factual input, query, targets and action hashes/labels match C5 training-stream rows. The raw query hashes must be72 unique.
- Evaluate factual and support-cleared versions (144 inputs,576 action outputs) for each checkpoint, physical/effective batch1, exactly4 inference loops, zero optimizer updates. Every target/value remains identical within the pair.
- Scope caveat:48 of72 queries were trained at depths1 or2. This is seen-input behavior at the deployment depth4, not exact original-depth fitting for those rows. Report original-depth strata explicitly; do not infer the learning dynamics from a single final checkpoint.
- Model/task/loss/initializer/predict/inspected_predict source byte-unchanged; no LLM, ARC data, planning, retraining or new learned representation.

## Controls, metrics and analysis frozen before readout

Primary factual policy accuracy, CE, empirical best constant action, advantage over that baseline. Primary full successor exact fraction overall and separately blocked/nonterminal/terminal; copy-current and goal-completion template baselines; fraction of predictions equal to the template; changed, unchanged, vacated and destination accuracy. Secondary cleared counterparts, reward precision/recall/Brier (threshold0.5), value MSE versus constant1 baseline0. Per-temporal-block and original-depth factual policy/state counts are descriptive, with no separate promotion claims.

Preserve raw pixel probability arrays, current/target/predicted states and every policy/reward/value readout. Recompute visible-grid oracle, regions, offsets, lengths, hashes, normalization and argmax independently. Verify every checkpoint remains unchanged and no update is logged. Reuse validated source-independent oracle helpers only with pinned hashes; no adapting input identities to pretend they are unseen.

For aggregate metrics use10,000 whole-query PCG64 resamples, seed1911,95% linear percentile intervals. Resample complete paired factual/cleared query clusters. Recompute the best constant action within each draw. Count undefined zero-denominator draws; report undefined intervals if any such draw occurs. All-zero empirical bootstrap intervals are not population proofs. No seed/checkpoint/metric selection after outputs; initial/final is the entire registered pair. Exploratory subdivisions cannot rescue an aggregate failure.

Descriptive retained competence gates: factual policy>=90% and lower95%CI advantage over resampled best constant strictly>25percentage points; full successor exact>=75% overall and each nonempty blocked/nonterminal/terminal class. Both required for a seen-panel pass; only a single-seed diagnostic, never architecture promotion. If either fails, prioritize a registered optimization/representation premise check; the current concrete candidate is cohort-preserving replay. If both pass, quantify the separate C5 unseen failure without assuming forgetting, capacity or depth as its cause. If integrity fails, repair it before interpreting metrics. No automatic dependent training.

## Provenance, resources and stop rule

Build from a clean reviewed pushed exact commit with cudnn,profiling,serde_json/float_roundtrip; retain separately hashed binary and exact sibling1ea5cc5c5998a1c2bd388eb158653dd0d1b7452a. Before the panel, run CPU input audit and old64-query frozen evaluation of C5 initial weights; old raw rows/states/probabilities must match the parent outputs exactly, report legacy fields identical except durations and new top-level argv/provenance. That is the exact CUDA/profiler smoke and compatibility check. A new flag must be rejected outside dedicated evaluation/audit modes.

Budget: CPU audit<=300s; each GPU frozen invocation<=300s;3 expected invocations (legacy parity, initial seen, final seen), typically seconds each; no optimizer or batch-capacity test because physical1 uses the already qualified inference path. Require512MiB minimum sampled reserve, AC power, all wired profilers: first representative evaluation Candle Graph capture, unique host trace, NVTX/Nsight CUDA/NVTX/OSrt/cuDNN/cuBLAS/CPU sampling, official CSV reports, separately Nsight-bound bundle. Freeze legacy/default semantics; missing operation/activation linkage, allocation lifetimes/physical checkpoints/device event intervals and incomplete automatic NVTX correlation remain explicit gaps. Profiled work does not support timing claims.

After subprocesses/telemetry exit, hash and verify every run and complete campaign; retain the manifest digest outside the root. Record implementation failures separately, never reuse a partial root. Stop this bounded diagnostic after one verified initial/final pair and saved decision; completing it does not stop the user's broader autonomous model work.
